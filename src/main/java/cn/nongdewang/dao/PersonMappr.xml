<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 为这个mapper指定一个唯一的namespace，namespace的值习惯上设置成包名+sql映射文件名，这样就能够保证namespace的值是唯一的 -->

<mapper namespace="cn.nongdewang.dao.PersonMapper"  >
	<resultMap type="cn.nongdewang.entity.Person" id="findAllResult">
		<id property="id" column="id"/>
		<result property="name" column="name"/>
		<result property="password" column="password"/>
	</resultMap>
	<select id="findAllPersons" resultMap="findAllResult"  >
	select  p.name,p.password,p.id from person p
	</select> 
	
	<!-- 保存用户 -->
	<insert id="save" parameterType="person">
	insert into person value(#{id,jdbcType=varchar},#{name,jdbcType=varchar},#{password,jdbcType=varchar})
	</insert>
</mapper>